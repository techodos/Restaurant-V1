# Migrations are checksummed over their raw bytes (scripts/db/migrate.mjs): always check out with LF,
# otherwise core.autocrlf on Windows changes the hash and the runner reports "modified after being applied".
db/migrations/*.sql text eol=lf
