# restaurant-platform

Before changing code, read `docs/skills/restaurant-platform/SKILL.md` (architecture map, layer rules, where new code goes, symbol map, gotchas) instead of scanning the project. Longer rationale: `docs/ARCHITECTURE.md`, `DECISIONS.md`.

After structural changes run `npm run typecheck` and `npx vitest run tests/architecture.test.ts`.
